<% provide(:title, @user.name) %>
<% content_for(:head)  do %>
<script>
    $(document).ready(function() {
        $("a.fancybox").fancybox({type: 'image'});
        $('#photo-info').stickyMojo({footerID: '.footer', contentID: '#profile-info'});
    });
</script>

<% end %>
	<div id="wrapper">
    <div id="profile">
      <div id="photo-info">
      <h4><%= @user.name %></h4>
      <div id="photo">
          <% if @profile_pic == nil %>
            <%= gravator_for @user %>
          <% else %>
            <%= image_tag @profile_pic.image.url(:medium) %> 
          <% end %>
      </div>
      <table width="100%">
        <tr>
          <td width="50%" align="center"><button class="btn btn-block" type="button"><i class="icon-thumbs-up"></i> 15</button>
</td>
          <td width="50%" align="center"><button class="btn btn-block" type="button"><i class="icon-thumbs-down"></i> 5</button></td>
        </tr>
      </table>
    <div class="spacer"></div>
      <div class="rating">Quick Stats</div>
      <p><b>Member Since: </b><%= @user.created_at.strftime("%d %b %Y") %></p>
      <p>Last Seen: 8 hours ago</p>
      </div>
      <div id="profile-info">
      	<% if flash[:success] %>
      	   <div class="alert alert-success"><a class="close" data-dismiss="alert" href="#">&times;</a><%= flash[:success] %></div>
      	<% end %>
        <div class="rating">
        	<span class="pull-left">Average Rating</span>
       	  <div class="clear"></div>
        </div>
        <table width="617">
          <tr>
          <td width="29%" height="130" align="center"><%= image_tag("star.png",width: 128, height: 128, alt: "star")%></td>
          <td width="71%" colspan="2" rowspan="2" valign="top"><table width="100%" cellpadding="5" class="table-hover">
            <tr>
              <td width="36%"><strong>Total Votes</strong></td>
              <td width="64%"> 1500</td>
            </tr>
            <tr>
              <td><strong>Profile Views</strong></td>
              <td>2536</td>
            </tr>
            <tr>
              <td><strong>Current Position</strong></td>
              <td>#85</td>
            </tr>
          </table>
<br /><p class="smart-title">Rank Cynthia</p>
            <form id="form1" name="form1" method="post" action="">
              <p>
                <label for="ratebox"></label>
                <select name="ratebox" id="ratebox" class="span4">
                  <option>Give your rating</option>
                  <option value="1">1 - Not attractive</option>
                  <option value="2">2 - Respectable</option>
                  <option value="3">3 - Decent or Attractive</option>
                  <option value="4">4 - Girlfriend Material</option>
                  <option value="5">5 - Super Hottie</option>
                </select>
              </p>
          </form>
            <p><b>Average Overview </b> <span class="label label-warning">Girlfriend Material</span></p></td>
          </tr>
          <tr>
            <td height="83" align="center" valign="middle"><span class="clear"><span class="big-rating">85%</span></span></td>
          </tr>
        </table>
    <div class="spacer"></div>
    <div class="rating">About Me</div>
    <p>Write a few words about yourself - your personality, temperament, attitude towards life and people. What makes you stand out from the crowd? What do other people love about you? And what drives them crazy?</p>
        <div class="spacer"></div>
      <div class="rating">All Photos</div>
      <% @user.photos.each do |photo| %>
        <%= link_to(image_tag(photo.image.url(:thumb)), photo.image.url(:original), class: "fancybox", rel: "gallery1") %>              
      <% end %>        
      <hr />
      <h4>Leave a comment</h4>
      <%= form_for([@user,@comment]) do |f| %>
        <% if flash[:error] %>
            <div class="alert alert-error"><a class="close" data-dismiss="alert" href="#">&times;</a><%= flash[:error] %></div>
        <% end %>
        <%= f.text_area :content, cols: 45, rows: 3, class: "btn-block comment-box" %>
        <%= f.submit "Comment", class: "btn" %><div class="rating pull-right"><%= image_tag "comment.png" %> <%= pluralize(@user.comments.count, "comment") %> so far</div>
      <% end %>
      <% if @comments.any? %>
        <div class="comments">
            <%= render @comments %>
      <% else %>
        <p>No comments yet! be the first</p>
      <% end %>
      </div>
      
   
      </div>
    <div class="clear"></div>
    </div>
    <div class="clear"></div>

</div>
